package ru.spbstu.telematics.java;

import junit.framework.TestCase;
import junit.framework.TestSuite;
import org.junit.Assert;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

import javax.swing.*;
import java.util.Collection;
import java.util.Stack;

import static org.junit.Assert.*;

/**
 * Unit test for simple App.
 */
public class AppTest 
    extends TestSuite
{
    /*private Stack<Integer> javaStack;
    private MyStack<Integer> myStack;

    private<T> Boolean CollectionsAreEquals(Collection<T> c1, Collection<T> c2 ){
        return c1.equals(c2);
    }

    @Before
    public static void SetUp(){
        Stack<Integer> javaStack = new Stack<Integer>();
        MyStack<Integer> myStack = new MyStack<Integer>();
    }







    @Test
    public void TestPush(){


        javaStack.push(1);
        javaStack.push(2);
        javaStack.push(4);
        javaStack.push(7);
        javaStack.push(8);

        myStack.push(1);
        myStack.push(2);
        myStack.push(4);
        myStack.push(7);
        myStack.push(8);

        Assert.assertTrue(CollectionsAreEquals(myStack, javaStack));


        javaStack.remove((Object)7);
        myStack.remove(7);
        Assert.assertTrue(CollectionsAreEquals(myStack, javaStack));



        Assert.assertArrayEquals(javaStack.toArray(),myStack.toArray());

        Assert.assertTrue(javaStack.search(1)==myStack.search(1));



    }


     */


}
